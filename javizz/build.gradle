/*
 * This build file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.3/userguide/java_library_plugin.html
 */

buildscript {
    // nécessaire s"il y a des dépendences de build gradle (build script)

    //repositories {
    //    maven { url uri('https://maven.openflexo.org/artifactory/openflexo-deps/') }
    //}
    dependencies {
        //classpath group: 'org.openflexo', name: 'buildplugin', version: '0.2'
        //classpath group: 'org.openflexo', name: 'buildconfig', version: '2.0.0'
        //classpath 'org.anarres.gradle:gradle-sablecc-plugin:[1.0.5,)'
    }
}

// Apply the java-library plugin to add support for Java Library
apply plugin: 'java-library'

repositories {
    maven { url uri('https://maven.openflexo.org/artifactory/openflexo-deps/') }
}

dependencies {

    // This dependency is exported to consumers, that is to say found on their compile classpath.
    //api 'org.apache.commons:commons-math3:3.6.1'

    // This dependency is used internally, and not exposed to consumers on their own compile classpath.
    //implementation 'com.google.guava:guava:23.0'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'

    compile group: 'org.openflexo', name: 'pamela-core', version: '1.5-SNAPSHOT'
    compile group: 'org.openflexo', name: 'flexo-foundation', version: '2.0.0-SNAPSHOT'
    implementation 'com.github.javaparser:javaparser-core:3.14.10'

}

apply plugin: 'application'

mainClassName = 'javizz.Testing'
